name: Daily Bhavcopy

on:
  schedule:
    - cron: '30 21 * * *'   # 3:00 AM IST
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install libs
        run: |
          pip install pandas requests gspread google-auth

      - name: Run Bhavcopy
        env:
          GSHEET_CREDS: ${{ secrets.GSHEET_CREDS }}
        run: python bhavcopy.py
